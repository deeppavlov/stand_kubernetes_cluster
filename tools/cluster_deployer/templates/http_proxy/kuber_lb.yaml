apiVersion: v1
kind: Service
metadata:
  name: {{KUBER_LB_NAME}}
spec:
  selector:
    app: {{KUBER_DP_NAME}}
  type: LoadBalancer
  externalIPs:
  - {{CLUSTER_IP}}
  ports:
  - name: lb-listened
    protocol: TCP
    port: {{CLUSTER_PORT}}
    targetPort: dp-listened