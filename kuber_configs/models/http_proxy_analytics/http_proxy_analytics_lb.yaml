apiVersion: v1
kind: Service
metadata:
  name: http-proxy-analytics-lb
  namespace: stand-demo
spec:
  selector:
    app: http-proxy-analytics-dp
  type: LoadBalancer
  externalIPs:
  - 10.11.1.251
  ports:
  - name: lb-listened
    protocol: TCP
    port: 7022
    targetPort: dp-listened