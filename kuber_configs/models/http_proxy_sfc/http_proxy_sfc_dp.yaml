apiVersion: apps/v1
kind: Deployment
metadata:
  name: http-proxy-sfc-dp
  namespace: stand-demo
spec:
  selector:
    matchLabels:
      app: http-proxy-sfc-dp
  replicas: 1
  template:
    metadata:
      labels:
        app: http-proxy-sfc-dp
    spec:
      containers:
      - name: nginx
        image: kubeadm.ipavlov.mipt.ru:5000/stand/http_proxy
        ports:
        - name: dp-listened
          protocol: TCP
          containerPort: 7068
        env:
        - name: LISTENED_PORT
          value: "7068"
        - name: PROXYED_HOST
          value: "10.11.1.1"
        - name: PROXYED_PORT
          value: "8107"
      dnsPolicy: "None"
      dnsConfig:
        nameservers:
          - 10.11.1.254