apiVersion: v1
kind: Service
metadata:
  name: http-proxy-sfc-lb
  namespace: stand-demo
spec:
  selector:
    app: http-proxy-sfc-dp
  type: LoadBalancer
  externalIPs:
  - 10.11.1.251
  ports:
  - name: lb-listened
    protocol: TCP
    port: 7068
    targetPort: dp-listened