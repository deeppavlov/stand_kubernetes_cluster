apiVersion: v1
kind: Service
metadata:
  name: http-proxy-max-lb
  namespace: stand-demo
spec:
  selector:
    app: http-proxy-max-dp
  type: LoadBalancer
  externalIPs:
  - 10.11.1.251
  ports:
  - name: lb-listened
    protocol: TCP
    port: 7088
    targetPort: dp-listened