apiVersion: apps/v1
kind: Deployment
metadata:
  name: http-proxy-oasst12b-dp
  namespace: stand-demo
spec:
  selector:
    matchLabels:
      app: http-proxy-oasst12b-dp
  replicas: 1
  template:
    metadata:
      labels:
        app: http-proxy-oasst12b-dp
    spec:
      containers:
      - name: nginx
        image: kubeadm.ipavlov.mipt.ru:5000/stand/http_proxy
        ports:
        - name: dp-listened
          protocol: TCP
          containerPort: 8158
        env:
        - name: LISTENED_PORT
          value: "8158"
        - name: PROXYED_HOST
          value: "10.11.1.8"
        - name: PROXYED_PORT
          value: "8158"
      dnsPolicy: "None"
      dnsConfig:
        nameservers:
          - 10.11.1.254