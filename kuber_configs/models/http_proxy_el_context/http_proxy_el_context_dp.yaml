apiVersion: apps/v1
kind: Deployment
metadata:
  name: http-proxy-el-context-dp
  namespace: stand-demo
spec:
  selector:
    matchLabels:
      app: http-proxy-el-context-dp
  replicas: 1
  template:
    metadata:
      labels:
        app: http-proxy-el-context-dp
    spec:
      containers:
      - name: nginx
        image: kubeadm.ipavlov.mipt.ru:5000/stand/http_proxy
        ports:
        - name: dp-listened
          protocol: TCP
          containerPort: 7030
        env:
        - name: LISTENED_PORT
          value: "7030"
        - name: PROXYED_HOST
          value: "10.11.1.6"
        - name: PROXYED_PORT
          value: "8175"
      dnsPolicy: "None"
      dnsConfig:
        nameservers:
          - 10.11.1.254