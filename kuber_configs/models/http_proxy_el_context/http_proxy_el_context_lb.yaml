apiVersion: v1
kind: Service
metadata:
  name: http-proxy-el-context-lb
  namespace: stand-demo
spec:
  selector:
    app: http-proxy-el-context-dp
  type: LoadBalancer
  externalIPs:
  - 10.11.1.251
  ports:
  - name: lb-listened
    protocol: TCP
    port: 7030
    targetPort: dp-listened