apiVersion: apps/v1
kind: Deployment
metadata:
  name: http-proxy-bloom-dp
  namespace: stand-demo
spec:
  selector:
    matchLabels:
      app: http-proxy-bloom-dp
  replicas: 1
  template:
    metadata:
      labels:
        app: http-proxy-bloom-dp
    spec:
      containers:
      - name: nginx
        image: kubeadm.ipavlov.mipt.ru:5000/stand/http_proxy
        ports:
        - name: dp-listened
          protocol: TCP
          containerPort: 8146
        env:
        - name: LISTENED_PORT
          value: "8146"
        - name: PROXYED_HOST
          value: "10.11.1.8"
        - name: PROXYED_PORT
          value: "8146"
      dnsPolicy: "None"
      dnsConfig:
        nameservers:
          - 10.11.1.254