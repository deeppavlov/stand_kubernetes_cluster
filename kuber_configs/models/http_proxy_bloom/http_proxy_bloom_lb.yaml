apiVersion: v1
kind: Service
metadata:
  name: http-proxy-bloom-lb
  namespace: stand-demo
spec:
  selector:
    app: http-proxy-bloom-dp
  type: LoadBalancer
  externalIPs:
  - 10.11.1.251
  ports:
  - name: lb-listened
    protocol: TCP
    port: 8146
    targetPort: dp-listened