apiVersion: apps/v1
kind: Deployment
metadata:
  name: http-proxy-entity-linking-dp
  namespace: stand-demo
spec:
  selector:
    matchLabels:
      app: http-proxy-entity-linking-dp
  replicas: 1
  template:
    metadata:
      labels:
        app: http-proxy-entity-linking-dp
    spec:
      containers:
      - name: nginx
        image: kubeadm.ipavlov.mipt.ru:5000/stand/http_proxy
        ports:
        - name: dp-listened
          protocol: TCP
          containerPort: 7075
        env:
        - name: LISTENED_PORT
          value: "7075"
        - name: PROXYED_HOST
          value: "10.11.1.8"
        - name: PROXYED_PORT
          value: "8075"
      dnsPolicy: "None"
      dnsConfig:
        nameservers:
          - 10.11.1.254