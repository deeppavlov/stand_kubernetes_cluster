apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: demo-squad-ru-confidence-dp
  namespace: stand-demo
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: demo-squad-ru-confidence-dp
    spec:
      containers:
      - name: demo-squad-ru-confidence
        image: kubeadm.ipavlov.mipt.ru:5000/demo/squad_ru_confidence
        resources:
          limits:
            nvidia.com/gpu: 1
        ports:
        - name: cp6019
          protocol: TCP
          containerPort: 6019
        volumeMounts:
        - name: volume-logs
          mountPath: /logs
        - name: volume-podinfo
          mountPath: /etc/podinfo
          readOnly: false
        env:
        - name: POD_NODE
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
      volumes:
      - name: volume-logs
        persistentVolumeClaim:
          claimName: logs-hostpath-pvc
      - name: volume-podinfo
        downwardAPI:
          items:
          - path: "labels"
            fieldRef:
              fieldPath: metadata.labels
          - path: "annotations"
            fieldRef:
              fieldPath: metadata.annotations
