apiVersion: apps/v1
kind: Deployment
metadata:
  name: http-proxy-jupyter-dp
  namespace: stand-demo
spec:
  selector:
    matchLabels:
      app: http-proxy-jupyter-dp
  replicas: 1
  template:
    metadata:
      labels:
        app: http-proxy-jupyter-dp
    spec:
      containers:
      - name: nginx
        image: kubeadm.ipavlov.mipt.ru:5000/stand/http_proxy
        ports:
        - name: dp-listened
          protocol: TCP
          containerPort: 7070
        env:
        - name: LISTENED_PORT
          value: "7070"
        - name: PROXYED_HOST
          value: "10.11.1.4"
        - name: PROXYED_PORT
          value: "8887"
      dnsPolicy: "None"
      dnsConfig:
        nameservers:
          - 10.11.1.254