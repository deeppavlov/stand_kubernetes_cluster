apiVersion: v1
kind: Service
metadata:
  name: http-proxy-jupyter-lb
  namespace: stand-demo
spec:
  selector:
    app: http-proxy-jupyter-dp
  type: LoadBalancer
  externalIPs:
  - 10.11.1.251
  ports:
  - name: lb-listened
    protocol: TCP
    port: 7070
    targetPort: dp-listened