apiVersion: apps/v1
kind: Deployment
metadata:
  name: http-proxy-demoa-dp
  namespace: stand-demo
spec:
  selector:
    matchLabels:
      app: http-proxy-demoa-dp
  replicas: 1
  template:
    metadata:
      labels:
        app: http-proxy-demoa-dp
    spec:
      containers:
      - name: nginx
        image: kubeadm.ipavlov.mipt.ru:5000/stand/http_proxy
        ports:
        - name: dp-listened
          protocol: TCP
          containerPort: 7094
        env:
        - name: LISTENED_PORT
          value: "7094"
        - name: PROXYED_HOST
          value: "10.11.1.41"
        - name: PROXYED_PORT
          value: "3001"
      dnsPolicy: "None"
      dnsConfig:
        nameservers:
          - 10.11.1.254